// import { ApiList } from '../config';
// import SYNC from './sync';
// import _ from 'lodash';

export const createEvent = (name: string = '', data: any = null): void => {
    return;
    // return SYNC.fetch(
    //     {
    //         url: ApiList.eventLog,
    //         type: 'POST',
    //         data: {
    //             name: name,
    //             data: _.isString(data) ? data : JSON.stringify(data),
    //         },
    //     },
    //     'createEvent',
    //     false
    // );
};
